# ─────────────────────────────────────────────
#  Voidclip.mov — Python Dependencies
#  Python 3.12+
# ─────────────────────────────────────────────

# GUI Framework
PySide6>=6.7.0

# Video / Image Processing
opencv-python>=4.10.0
av>=13.0.0                 # PyAV — FFmpeg Python bindings

# Database
# SQLite3 is built-in to Python stdlib (no extra install)

# Utilities
psutil>=6.0.0              # System/process monitoring
send2trash>=1.8.3          # Safe file deletion
Pillow>=10.4.0             # Thumbnail generation / image utils

# Hashing / Crypto
xxhash>=3.4.1              # Fast file deduplication hashing

# Logging
colorlog>=6.8.2            # Colored console logs (dev mode)

# Type checking (dev only)
# mypy>=1.10.0
# types-Pillow
